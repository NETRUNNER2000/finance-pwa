[build]
  command = "pnpm install && pnpm build"
  publish = ".next"

[context.production.environment]
  NODE_ENV = "production"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"